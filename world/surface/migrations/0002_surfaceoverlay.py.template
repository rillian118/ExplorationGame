"""
Template migration only.

Prefer generating the real migration with:

    python manage.py makemigrations surface
    python manage.py migrate

Do not copy this blindly unless the dependency matches your project.
"""

from django.db import migrations, models
import django.db.models.deletion


class Migration(migrations.Migration):

    dependencies = [
        # Replace with the latest migration in your surface app.
        ("surface", "0001_initial"),
        # Evennia ObjectDB dependency may differ by installed version.
        ("objects", "0001_initial"),
    ]

    operations = [
        migrations.CreateModel(
            name="SurfaceOverlay",
            fields=[
                ("id", models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name="ID")),
                ("planet_key", models.CharField(db_index=True, max_length=128)),
                ("x", models.IntegerField(db_index=True)),
                ("y", models.IntegerField(db_index=True)),
                ("overlay_type", models.CharField(db_index=True, max_length=64)),
                ("name", models.CharField(blank=True, default="", max_length=160)),
                ("description", models.TextField(blank=True, default="")),
                ("data", models.JSONField(blank=True, default=dict)),
                ("is_permanent", models.BooleanField(default=True)),
                ("blocks_cleanup", models.BooleanField(default=True)),
                ("visible_on_surface", models.BooleanField(default=True)),
                ("created_at", models.DateTimeField(auto_now_add=True)),
                ("updated_at", models.DateTimeField(auto_now=True)),
                ("owner_object", models.ForeignKey(blank=True, help_text="Player, account-controlled character, organization object, or owner proxy.", null=True, on_delete=django.db.models.deletion.SET_NULL, related_name="surface_overlays_as_owner", to="objects.objectdb")),
                ("source_object", models.ForeignKey(blank=True, help_text="Object that physically/logically creates this overlay, such as a landed ship.", null=True, on_delete=django.db.models.deletion.SET_NULL, related_name="surface_overlays_as_source", to="objects.objectdb")),
            ],
            options={"ordering": ["planet_key", "x", "y", "overlay_type", "id"]},
        ),
        migrations.AddIndex(model_name="surfaceoverlay", index=models.Index(fields=["planet_key", "x", "y"], name="surfaceover_planet__idx")),
        migrations.AddIndex(model_name="surfaceoverlay", index=models.Index(fields=["planet_key", "x", "y", "overlay_type"], name="surfaceover_planet__idx2")),
        migrations.AddIndex(model_name="surfaceoverlay", index=models.Index(fields=["blocks_cleanup"], name="surfaceover_blocks__idx")),
        migrations.AddIndex(model_name="surfaceoverlay", index=models.Index(fields=["visible_on_surface"], name="surfaceover_visible_idx")),
    ]
